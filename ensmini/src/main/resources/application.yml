spring:
  config:
    activate:
      on-profile: local

  datasource:
    url: "jdbc:h2:mem:mini:MODE=MYSQL;NON_KEYWORDS=USER"
    username: "sa"
    password: ""
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        show_sql: true
        format_sql: true


  h2:
    console:
      enabled: true #
      path: /h2-console #
---
spring:
  config:
    activate:
      on-profile: dev

  datasource:
    url: "jdbc:mysql://localhost:3306/mini"
    username: "root"
    password: "1234"
    driver-class-name: com.mysql.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        show_sql: true
        format_sql: true

logging.level:
    org.hibernate.SQL: debug
  # org.hibernate.type: trace

server:
  port : 8081